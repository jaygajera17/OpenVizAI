# Frontend (apps/web)

This folder will contain the React frontend for openVizAI. Recommended starter:

- Vite (`npm create vite@latest apps/web -- --template react-ts`)
- or Create React App

After scaffolding, use `npm start` inside `apps/web` or run `npm run dev:web` from repository root.
